# DIABETES_PREDICTION
This is a Python-based machine learning project to predict whether a person is diabetic or not. I used patient health data, cleaned and prepared the data, trained a model, and tested its accuracy. The system takes user input and gives a clear diabetes prediction.
